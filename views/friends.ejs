<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/footer.css" />
    <link rel="stylesheet" href="/styles/global.css" />
    <link rel="stylesheet" href="/styles/responsive/nav.css" />
    <link rel="stylesheet" href="/styles/nav.css" />
    <link rel="stylesheet" href="/styles/friends.css" />
    <link rel="stylesheet" href="/styles/responsive/footer.css" />
    <link rel="stylesheet" href="/styles/loadingScreen.css" />
    <link rel="stylesheet" href="/styles/responsive/friends.css" />
    <link rel="shortcut icon" href="/img/logo.png" />
    <title>Приятели - Welldoers</title>
  </head>
  <body>
    <%- include('partials/loading-screen') %> <%- include('partials/header') %>

    <div class="welcome-area">
      <img src="/img/park.png" />
      <h1>Нашите партньори са и наши приятели</h1>
    </div>
    <div class="sub-info">
      <p class="sub-p">Наш'те хора :)</p>
      <h1 class="sub-h1">
        За партньори избираме само свежи хора с чувство за хумор
      </h1>
    </div>

    <section class="partners">
      <% Object.keys(data).forEach(partnerId => { %>
      <div class="partner-box">
        <% const jpgImageUrl =
        `https://your-storage-bucket-url/partner_images/${partnerId}.jpg` %> <%
        const pngImageUrl =
        `https://your-storage-bucket-url/partner_images/${partnerId}.png` %> <%
        const imageUrl = imageUrls.find(img => img.name === `${partnerId}.jpg`
        || img.name === `${partnerId}.png`); %>
        <img
          class="partner-logo"
          src="<%= imageUrl ? imageUrl.imageUrl : pngImageUrl %>"
          alt="partner logo"
        />
        <h1><%= data[partnerId].name %></h1>
        <p><%= data[partnerId].website %></p>
        <!-- Add other partner details as needed -->
      </div>
      <% }); %>
    </section>

    <%- include('partials/footer') %>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.5/gsap.min.js"></script>
    <script src="/JS/mobileMenu.js"></script>
    <script src="/JS/loadingAnimation.js"></script>
    <script src="/JS/linksTransfer.js"></script>
  </body>
</html>
